import CommentModel from './Comment';
import TopicModel from './Topic';
import UserModel from './User';

import type { IComment, ITopic, IUser } from './types';

export { CommentModel, TopicModel, UserModel, IComment, ITopic, IUser };
